import styled from "styled-components";
import { grid, space, position, layout, flexbox } from "styled-system";

const Box = styled.div`
  ${grid}
  ${space}
    ${position}
    ${layout}
    ${flexbox}
`;

export default Box;
